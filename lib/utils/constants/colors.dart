import 'package:flutter/material.dart';

class MyColors{

  static const primaryLight = Color.fromRGBO(249, 249, 249, .94);

  static const secondaryLight = Color.fromRGBO(118, 118, 128, .12);

  static const backgroundLight = Colors.white;

  static const primaryDark = Color.fromRGBO(29, 29, 29, .94);

  static const secondaryDark = Color.fromRGBO(118, 118, 128, .24);

  static const backgroundDark = Colors.black;
  
  static const customBlue = Color.fromRGBO(0, 122, 255, 1);

  static const textColorLight = Color.fromRGBO(166, 166, 166, 1);

  static const textColorDark = Color.fromRGBO(235, 235, 245, .6);

  static const customButtonLight = Color.fromRGBO(241, 241, 241, 1);

  static const customButtonDark = Color.fromRGBO(44, 44, 46, 1);

  static const red = Color(0xFFFF0000);



}
